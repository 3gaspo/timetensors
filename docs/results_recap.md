# Finalized experiment recap

## Current evidence

No archived real-dataset artifact is eligible for reuse under the current
single-pass loss and prepared-dataset contracts. The historical partial
constants run had completion markers but no synchronized metric payload from
which a policy ranking could be reconstructed.

## Finalized conclusions

- No current real-dataset family ranking is finalized.
- The dependency-light synthetic benchmark validates orchestration and
  reporting only; it is not PatchTST, DLinear, or foundation-model evidence.
- Every desired publication configuration must be recomputed.

## Next evidence

Run one bare `test` path, then execute the numbered full studies beginning
with constants and sampling. Use their selected controls in the later
reference, loss, linear, and centralized/per-user studies.

The complete historical audit remains
[`executive_summary.pdf`](../latex/executive_summary.pdf).
